apiVersion: v1
kind: Service
metadata:
  name: taskflow-api-service
  namespace: default
  labels:
    app: taskflow-api
spec:
  type: ClusterIP
  selector:
    app: taskflow-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
